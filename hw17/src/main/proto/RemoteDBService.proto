syntax = "proto3";

package ru.dvsokolov.protobuf.generated;

option java_multiple_files = true;

message Counter {
    int64 value = 1;
}

message Interval {
    int64 start = 1;
    int64 end = 2;
}

service RemoteDBService {
    rpc startCount(Interval) returns (stream Counter);
}
